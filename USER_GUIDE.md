# DropSendTo ユーザーガイド

## 1. DropSendTo とは
DropSendTo は、Windows 上で常時表示される軽量ランチャです。ウィンドウ上のスロットへファイルやフォルダをドラッグ＆ドロップする、あるいはショートカットキーで呼び出すだけで、登録したコマンドやマクロを実行できます。エクスプローラーの「送る」に近い操作感を保ちながら、より柔軟な自動化を行えます。

## 2. 動作環境
- 対応 OS: Windows 10 22H2 以降 / Windows 11
- 必要ランタイム: .NET 8 Desktop Runtime（同梱バンドル版を使用する場合は不要）
- 入力デバイス: キーボード、マウス

## 3. セットアップ手順
1. 配布パッケージ（ZIP）をダウンロードして任意のフォルダに展開します。
2. 同梱の `.exe` を起動すると、半透明の 2×2 グリッドウィンドウが画面上に表示されます。初回起動時、設定ファイルが `%AppData%\DropSendTo\config.json` に自動生成されます。
3. Windows SmartScreen で警告が表示された場合は、「詳細情報」→「実行」を選択してください。

## 4. ウィンドウとレイヤーの基本
- 既定レイアウトは 2 行×2 列、レイヤーは 4 つ用意されています。
- ウィンドウ右上の数字ボタンでレイヤーを切り替えできます。マウスホイールでも前後のレイヤーへ循環切替できます。
- `Slot Layout` メニューから行列数を変更すると、最大 4×4 の 16 スロット×レイヤーを構成できます。
- `Slot Size` メニューでスロットの大きさを Large / Small から選択できます。サイズを切り替えるとスロット表示やウィンドウの幅・高さも自動調整されます。
- ウィンドウは常に最前面で表示され、`Always on Top` メニューのチェックを外すと通常ウィンドウになります。

## 5. スロットの登録方法
1. 任意のスロットを **右クリック** → `Edit` を選択します。
2. 以下の項目を設定します。
   - **Title**: スロットに表示する名前。
   - **Command**: 実行するアプリケーションやスクリプトのパス。
   - **Arguments**: `{args}` プレースホルダを使ってドロップされたパスを挿入できます。
   - **Macro Script**: キーボード/マウス操作を事前に送信したい場合に記述します。
   - **Shortcut**: Prefix 入力後に呼び出すショートカットです（例: `Ctrl+Shift+1`）。
3. `Command` と `Macro Script` はどちらか片方だけでも構いません。
   - マクロなし（コマンドのみ）のスロットは、ほかのマクロが実行中でもショートカットやクリックで呼び出せます。マクロ付きスロットは同時に 1 件ずつ実行され、実行中に別スロットを起動すると警告が表示されます。
4. `?` ボタンを押すと、対応コマンド一覧を別ウィンドウで参照できます。ウィンドウを閉じなくても編集に戻れます。
5. `OK` で保存すると、設定が `%AppData%\DropSendTo\config.json` に記録されます。

### 引数テンプレートのプレースホルダ
- `{args}`: ドロップまたは CLI で渡されたパスをスペース区切りで展開します（空白を含む場合は自動的に引用を付与）。
- `{clipboard}`: クリップボードの文字列をそのまま挿入します（前後の空白は自動的に除去されます）。
- `{clipboard_args}`: クリップボードにコピーしたパス（複数行でも可）を 1 件ずつ解析し、必要に応じて引用を付けて展開します。
  - 例: エクスプローラーで「パスのコピー」を実行してからスロットを起動すると、そのパスを引数に実行できます。

## 6. ファイル／フォルダの起動
- スロットにファイルまたはフォルダをドラッグ＆ドロップすると、`{args}` が置換されたコマンドが実行されます。
- ドロップ対象が未登録のスロットだった場合はエラーメッセージが表示されます。
- コマンドラインから `DropSendTo.exe "<path>"` のように実行すると、現在レイヤーで最初に登録されているスロットを使って同様に起動します。

## 7. Prefix とショートカットの使い方
1. `Change Prefix...` メニューで Prefix（例: `Ctrl+Q`）を設定します。
2. Prefix キーを押すとウィンドウ左上に「PREFIX」インジケーターが点灯し、4 秒間ショートカット待機状態になります。
3. スロットにショートカット（例: `Ctrl+X`）を登録している場合、Prefix を押した直後に **Ctrl を押し直さずに** `X` を押すだけで起動できます。もちろん Ctrl を離して押し直しても正しく動作します。
4. Prefix をもう一度押すとショートカット待機を解除し、Prefix キーを前面アプリへ送出します。

## 8. マクロスクリプトの基本
- `KEY Ctrl+C` のように記述するとキーボード操作を送信します。
- `PREFIX` / `PREFIX SEND` / `PREFIX ARM` と書くと、DropSendTo の Prefix（例: `Ctrl+Q`）を擬似入力してショートカット待機状態を起動できます。
- `PREFIX PASSTHROUGH` と書くと、Prefix をそのまま前面アプリケーションへ送信します。
- `WAIT 200` で 200 ミリ秒待機、`TEXT processed` で任意文字列を入力できます。
- マウス操作 (`MOUSELEFTCLICK`, `MOUSEMOVEABS`, `MOUSESCROLLDOWN 2` 等) にも対応しています。
- `REPEAT 3` 〜 `ENDREPEAT` でブロックを繰り返し実行できます（最大 1000 回）。
- `SET Name Value` で変数を定義し、任意のコマンド引数に `{{Name}}` と書くと展開されます。`UNSET Name` で削除できます。
- `ADD`/`SUB`/`MUL`/`DIV Name 1` のように記述すると 64bit 整数として四則演算を行えます。`APPEND`/`PREPEND` で文字列を末尾/先頭に結合できます。
- マクロ実行中にエラーが発生した場合、処理は中断されログに記録されます。

## 9. 設定・ログの保存場所
- 設定: `%AppData%\DropSendTo\config.json`（編集後はアプリを再起動すると反映されます）
- バックアップ: `%AppData%\DropSendTo\config.json.bak`
- ログ: `%AppData%\DropSendTo\logs\app.log`（約 1MB でローテーション、7 日より古いファイルは自動削除）

## 10. よくある質問と対処
| 症状 | 対処 |
| ---- | ---- |
| ショートカットが反応しない | Prefix が正しく設定されているか確認し、ショートカット欄が空になっていないかチェック。Ctrl 等を押し直さなくても動作しますが、押し直しても試してください。 |
| マクロだけ実行されてコマンドが起動しない | マクロ内でエラーが発生している可能性があります。`%AppData%\DropSendTo\logs` を確認してください。 |
| ウィンドウが見当たらない | 常時最前面のチェックを外している、または画面外に移動している可能性があります。`config.json` の `WindowLeft/WindowTop` を調整するか、アプリを再起動してください。 |
| 設定を初期化したい | アプリを終了し、`config.json` を削除すると次回起動時に既定値で再生成されます。必要に応じて `.bak` をコピーしてください。 |

## 11. サポート
- 既知の問題や改善要望はプロジェクトリポジトリの Issue に記載されています。
- 不具合を報告する際は、発生手順とログ (`%AppData%\DropSendTo\logs`) を添えてご連絡ください。

---
本ガイドは最終更新時点（最新版コミット）での機能を記載しています。バージョンアップに伴う UI・機能変更がある場合は、リリースノートも併せてご確認ください。
