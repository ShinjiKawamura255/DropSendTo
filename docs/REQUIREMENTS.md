# REQUIREMENTS

## Background & Goal
Windows 用の軽量ドラッグ＆ドロップランチャを .NET 8（Windows 側）で実装する。エクスプローラーの SendTo 的な使い勝手で、登録スロット（2x2、4レイヤー＝最大16件）へドロップすると関連アプリ／ショートカットを対象引数付きで起動する。

## Scope
- In: スロット登録/起動、レイヤー切替、常時最前面・半透明ウィンドウ、右クリックメニュー、設定永続化、適切なエラーハンドリング。
- Out: インストーラー/自動更新、クラウド同期、国際化、クロスプラットフォーム最適化。

## Use Cases
- UC-01: ファイル/フォルダをスロットへドロップして関連アプリを起動。
- UC-02: 右クリックからスロットにアプリ/ショートカット/引数を登録/編集/解除。
- UC-03: レイヤー（1-4）を切り替えて別の 2x2 セットを利用。

## Functional Requirements (FR)
- FR-001: 2x2 スロット×4レイヤー＝最大16登録をサポート。
- FR-002: ドロップされたパスを起動先の引数として渡す。
- FR-003: 右クリックメニューで登録/編集/解除/終了を提供。
- FR-004: 設定を永続化し、起動時に復元。
- FR-005: SendTo 相当の動作（コマンドライン引数でも対象パスを受理）。
- FR-006: エラーはユーザーに分かる形で通知し、安全に復帰。

## Non-Functional Requirements (NFR)
- NFR-001: .NET 8（Windows）で動作。WSL 上の .NET は不使用。
- NFR-002: Windows 10 22H2 以降で安定動作。起動 < 1s（温スタート）。
- NFR-003: 常時最前面、黒基調、半透明（文字は読みやすさを維持）。
- NFR-004: 設定はユーザープロファイル内（%AppData%）に保存。

## Constraints (CON)
- CON-001: UI フレームワークは Windows デスクトップ（WPF 推奨）。
- CON-002: ネットワーク不要。標準権限で動作。

## Acceptance Criteria (AC)
- AC-001: 16 スロットの登録/起動/解除が再起動後も保持される。
- AC-002: ドロップ/コマンド引数の双方で対象を起動できる。
- AC-003: ウィンドウは常時最前面、半透明で文字の視認性を損なわない。
- AC-004: 例外時はアプリが落ちず、エラーダイアログ/ログを出す。

## Traceability (excerpt)
- FR-001 → SP-001, SP-003 → DES-002 → TC-010/011
- FR-002 → SP-004 → DES-004 → TC-020/021
- NFR-001 → SP-008 → DES-001 → TC-001
