<!-- Custom dark window chrome (title bar + system buttons). Apply style to a Window and add SystemCommands bindings in code-behind. -->
<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:dwc="clr-namespace:System.Windows.Shell;assembly=PresentationFramework">
    <!-- Title bar buttons -->
    <Style x:Key="TitleBarButtonStyle" TargetType="Button">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderBrush" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Foreground" Value="{StaticResource Theme.PrimaryTextBrush}"/>
        <Setter Property="Width" Value="36"/>
        <Setter Property="Height" Value="28"/>
        <Setter Property="Padding" Value="0"/>
        <Setter Property="HorizontalContentAlignment" Value="Center"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="WindowChrome.IsHitTestVisibleInChrome" Value="True"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="Bd"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}">
                        <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="{StaticResource Theme.ControlHoverBackgroundBrush}"/>
                            <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource Theme.ControlHoverBorderBrush}"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="{StaticResource Theme.ControlPressedBackgroundBrush}"/>
                            <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource Theme.ControlPressedBorderBrush}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="CloseTitleBarButtonStyle" BasedOn="{StaticResource TitleBarButtonStyle}" TargetType="Button">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="Bd"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}">
                        <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="#AAFF0000"/>
                            <Setter TargetName="Bd" Property="BorderBrush" Value="#FFFF4444"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="#CCFF2222"/>
                            <Setter TargetName="Bd" Property="BorderBrush" Value="#FFFF8888"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Dark custom chrome for a Window -->
    <Style x:Key="DarkWindowChromeStyle" TargetType="Window">
        <Setter Property="WindowStyle" Value="None"/>
        <Setter Property="AllowsTransparency" Value="False"/>
        <Setter Property="ResizeMode" Value="CanResize"/>
        <Setter Property="Background" Value="{StaticResource Theme.WindowBackgroundBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource Theme.PrimaryTextBrush}"/>
        <Setter Property="dwc:WindowChrome.WindowChrome">
            <Setter.Value>
                <dwc:WindowChrome CaptionHeight="32"
                                  CornerRadius="0"
                                  GlassFrameThickness="0"
                                  ResizeBorderThickness="6"
                                  UseAeroCaptionButtons="False"/>
            </Setter.Value>
        </Setter>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Window">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{StaticResource Theme.BorderBrush}"
                            BorderThickness="1"
                            SnapsToDevicePixels="True">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="32"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <!-- Title bar -->
                            <Grid Grid.Row="0"
                                  Background="{StaticResource Theme.SurfaceAltBackgroundBrush}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="32"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <ContentPresenter Grid.Column="0"
                                                  Margin="8,0,4,0"
                                                  VerticalAlignment="Center"
                                                  Content="{TemplateBinding Icon}"
                                                  RecognizesAccessKey="True"
                                                  Width="16"
                                                  Height="16"
                                                  SnapsToDevicePixels="True"/>

                                <TextBlock Grid.Column="1"
                                           VerticalAlignment="Center"
                                           Text="{TemplateBinding Title}"
                                           Foreground="{TemplateBinding Foreground}"
                                           FontWeight="SemiBold"
                                           TextTrimming="CharacterEllipsis"/>

                                <StackPanel Grid.Column="2"
                                            Orientation="Horizontal"
                                            Background="{StaticResource Theme.SurfaceAltBackgroundBrush}">
                                    <Button Command="{x:Static SystemCommands.MinimizeWindowCommand}"
                                            CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"
                                            Style="{StaticResource TitleBarButtonStyle}"
                                            ToolTip="Minimize">
                                        <Path Data="M 0 0 L 10 0"
                                              Stroke="{TemplateBinding Foreground}"
                                              StrokeThickness="1.5"
                                              Stretch="Uniform"
                                              Width="10"
                                              Height="10"/>
                                    </Button>
                                    <Button x:Name="MaximizeButton"
                                            Command="{x:Static SystemCommands.MaximizeWindowCommand}"
                                            CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"
                                            Style="{StaticResource TitleBarButtonStyle}"
                                            ToolTip="Maximize/Restore">
                                        <Grid Width="12" Height="12">
                                            <Rectangle x:Name="MaximizeGlyph"
                                                       Stroke="{TemplateBinding Foreground}"
                                                       StrokeThickness="1.4"
                                                       RadiusX="0"
                                                       RadiusY="0"/>
                                            <Rectangle x:Name="RestoreGlyph"
                                                       Stroke="{TemplateBinding Foreground}"
                                                       StrokeThickness="1.4"
                                                       Visibility="Collapsed"
                                                       RadiusX="0"
                                                       RadiusY="0"
                                                       Width="10"
                                                       Height="9"
                                                       Margin="1,1,0,0"/>
                                        </Grid>
                                    </Button>
                                    <Button Command="{x:Static SystemCommands.CloseWindowCommand}"
                                            CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"
                                            Style="{StaticResource CloseTitleBarButtonStyle}"
                                            ToolTip="Close">
                                        <Path Data="M 0 0 L 8 8 M 8 0 L 0 8"
                                              Stroke="{TemplateBinding Foreground}"
                                              StrokeThickness="1.5"
                                              StrokeLineJoin="Round"
                                              StrokeStartLineCap="Round"
                                              StrokeEndLineCap="Round"
                                              Width="10"
                                              Height="10"/>
                                    </Button>
                                </StackPanel>
                            </Grid>

                            <!-- Window content -->
                            <AdornerDecorator Grid.Row="1">
                                <ContentPresenter Margin="0"
                                                  Content="{TemplateBinding Content}"
                                                  ContentTemplate="{TemplateBinding ContentTemplate}"
                                                  ContentTemplateSelector="{TemplateBinding ContentTemplateSelector}"
                                                  SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                            </AdornerDecorator>
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="WindowState" Value="Maximized">
                            <Setter TargetName="MaximizeButton" Property="Command" Value="{x:Static SystemCommands.RestoreWindowCommand}"/>
                            <Setter TargetName="MaximizeGlyph" Property="Visibility" Value="Collapsed"/>
                            <Setter TargetName="RestoreGlyph" Property="Visibility" Value="Visible"/>
                        </Trigger>
                        <Trigger Property="WindowState" Value="Normal">
                            <Setter TargetName="MaximizeButton" Property="Command" Value="{x:Static SystemCommands.MaximizeWindowCommand}"/>
                            <Setter TargetName="MaximizeGlyph" Property="Visibility" Value="Visible"/>
                            <Setter TargetName="RestoreGlyph" Property="Visibility" Value="Collapsed"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
