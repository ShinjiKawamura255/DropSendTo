<!-- Common dark styles: merge into App.xaml after Colors.xaml. Adjust sizing to suit your project. -->
<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <!-- System brush overrides to avoid light menu colors -->
    <SolidColorBrush x:Key="{x:Static SystemColors.MenuBrushKey}" Color="#FF1A1A1A"/>
    <SolidColorBrush x:Key="{x:Static SystemColors.MenuTextBrushKey}" Color="#FFFFFFFF"/>
    <SolidColorBrush x:Key="{x:Static SystemColors.HighlightBrushKey}" Color="#FF202020"/>
    <SolidColorBrush x:Key="{x:Static SystemColors.HighlightTextBrushKey}" Color="#FFFFFFFF"/>
    <SolidColorBrush x:Key="{x:Static SystemColors.WindowBrushKey}" Color="#FF111111"/>
    <SolidColorBrush x:Key="{x:Static SystemColors.ControlBrushKey}" Color="#FF111111"/>
    <SolidColorBrush x:Key="{x:Static SystemColors.ControlTextBrushKey}" Color="#FFFFFFFF"/>

    <!-- Base styles -->
    <Style TargetType="Window">
        <Setter Property="Background" Value="{StaticResource Theme.WindowBackgroundBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource Theme.PrimaryTextBrush}"/>
    </Style>

    <Style TargetType="TextBlock">
        <Setter Property="Foreground" Value="{StaticResource Theme.PrimaryTextBrush}"/>
    </Style>

    <Style TargetType="Label">
        <Setter Property="Foreground" Value="{StaticResource Theme.PrimaryTextBrush}"/>
    </Style>

    <Style TargetType="CheckBox">
        <Setter Property="Foreground" Value="{StaticResource Theme.PrimaryTextBrush}"/>
    </Style>

    <Style TargetType="Button">
        <Setter Property="Foreground" Value="{StaticResource Theme.PrimaryTextBrush}"/>
        <Setter Property="Background" Value="{StaticResource Theme.ControlBackgroundBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource Theme.ControlBorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="4,2"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="bd"
                            CornerRadius="8"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}">
                        <ContentPresenter HorizontalAlignment="Center"
                                          VerticalAlignment="Center"
                                          Margin="2"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="bd" Property="Background" Value="{StaticResource Theme.ControlHoverBackgroundBrush}"/>
                            <Setter TargetName="bd" Property="BorderBrush" Value="{StaticResource Theme.ControlHoverBorderBrush}"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="bd" Property="Background" Value="{StaticResource Theme.ControlPressedBackgroundBrush}"/>
                            <Setter TargetName="bd" Property="BorderBrush" Value="{StaticResource Theme.ControlPressedBorderBrush}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="bd" Property="Opacity" Value="0.5"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="TextBox">
        <Setter Property="Foreground" Value="{StaticResource Theme.PrimaryTextBrush}"/>
        <Setter Property="Background" Value="{StaticResource Theme.InputBackgroundBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource Theme.InputBorderBrush}"/>
        <Setter Property="SelectionBrush" Value="{StaticResource Theme.ControlHoverBorderBrush}"/>
        <Setter Property="CaretBrush" Value="{StaticResource Theme.PrimaryTextBrush}"/>
    </Style>

    <Style TargetType="PasswordBox">
        <Setter Property="Foreground" Value="{StaticResource Theme.PrimaryTextBrush}"/>
        <Setter Property="Background" Value="{StaticResource Theme.InputBackgroundBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource Theme.InputBorderBrush}"/>
        <Setter Property="CaretBrush" Value="{StaticResource Theme.PrimaryTextBrush}"/>
    </Style>

    <!-- ComboBox with dark toggle + popup -->
    <Style TargetType="ComboBox">
        <Setter Property="Foreground" Value="{StaticResource Theme.PrimaryTextBrush}"/>
        <Setter Property="Background" Value="{StaticResource Theme.InputBackgroundBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource Theme.InputBorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="4,2"/>
        <Setter Property="HorizontalContentAlignment" Value="Left"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="ItemContainerStyle">
            <Setter.Value>
                <Style TargetType="ComboBoxItem">
                    <Setter Property="Foreground" Value="{StaticResource Theme.PrimaryTextBrush}"/>
                    <Setter Property="Background" Value="{StaticResource Theme.SurfaceBackgroundBrush}"/>
                    <Setter Property="Padding" Value="6,2"/>
                    <Setter Property="HorizontalContentAlignment" Value="Left"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ComboBoxItem">
                                <Border x:Name="Bd"
                                        Background="{TemplateBinding Background}"
                                        Padding="{TemplateBinding Padding}">
                                    <ContentPresenter HorizontalAlignment="Left"
                                                      VerticalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="Bd" Property="Background" Value="{StaticResource Theme.ControlHoverBackgroundBrush}"/>
                                    </Trigger>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter TargetName="Bd" Property="Background" Value="{StaticResource Theme.DataGridRowSelectedBrush}"/>
                                    </Trigger>
                                    <Trigger Property="IsEnabled" Value="False">
                                        <Setter TargetName="Bd" Property="Opacity" Value="0.5"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </Setter.Value>
        </Setter>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ComboBox">
                    <Grid>
                        <ToggleButton x:Name="ToggleButton"
                                      Background="{TemplateBinding Background}"
                                      BorderBrush="{TemplateBinding BorderBrush}"
                                      BorderThickness="{TemplateBinding BorderThickness}"
                                      Focusable="False"
                                      OverridesDefaultStyle="True"
                                      IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                      ClickMode="Press"
                                      HorizontalContentAlignment="Stretch"
                                      VerticalContentAlignment="Center">
                            <ToggleButton.Template>
                                <ControlTemplate TargetType="ToggleButton">
                                    <Border x:Name="Bd"
                                            Background="{TemplateBinding Background}"
                                            BorderBrush="{TemplateBinding BorderBrush}"
                                            BorderThickness="{TemplateBinding BorderThickness}"
                                            CornerRadius="4">
                                        <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                                          SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="Bd" Property="Background" Value="{StaticResource Theme.ControlHoverBackgroundBrush}"/>
                                            <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource Theme.ControlHoverBorderBrush}"/>
                                        </Trigger>
                                        <Trigger Property="IsPressed" Value="True">
                                            <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource Theme.ControlPressedBorderBrush}"/>
                                        </Trigger>
                                        <Trigger Property="IsEnabled" Value="False">
                                            <Setter TargetName="Bd" Property="Opacity" Value="0.5"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </ToggleButton.Template>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <ContentPresenter x:Name="ContentSite"
                                                  Grid.Column="0"
                                                  Margin="4,0,6,0"
                                                  HorizontalAlignment="Left"
                                                  VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                                  Content="{Binding Path=SelectionBoxItem, RelativeSource={RelativeSource AncestorType=ComboBox}}"
                                                  ContentTemplate="{Binding Path=SelectionBoxItemTemplate, RelativeSource={RelativeSource AncestorType=ComboBox}}"
                                                  ContentTemplateSelector="{Binding Path=ItemTemplateSelector, RelativeSource={RelativeSource AncestorType=ComboBox}}"
                                                  SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                                <Path x:Name="Arrow"
                                      Grid.Column="1"
                                      Width="10"
                                      Height="6"
                                      Margin="0,0,6,0"
                                      VerticalAlignment="Center"
                                      HorizontalAlignment="Right"
                                      Stretch="Uniform"
                                      Fill="{StaticResource Theme.PrimaryTextBrush}"
                                      Data="M 0 0 L 5 5 L 10 0"/>
                            </Grid>
                        </ToggleButton>
                        <Popup x:Name="PART_Popup"
                               AllowsTransparency="True"
                               Focusable="False"
                               IsOpen="{TemplateBinding IsDropDownOpen}"
                               Placement="Bottom"
                               PopupAnimation="Fade">
                            <Border Background="{StaticResource Theme.SurfaceBackgroundBrush}"
                                    BorderBrush="{StaticResource Theme.BorderBrush}"
                                    BorderThickness="1"
                                    Padding="2"
                                    MinWidth="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType=ComboBox}}">
                                <ScrollViewer CanContentScroll="True"
                                              HorizontalScrollBarVisibility="Auto"
                                              VerticalScrollBarVisibility="Auto">
                                    <ItemsPresenter KeyboardNavigation.DirectionalNavigation="Cycle"/>
                                </ScrollViewer>
                            </Border>
                        </Popup>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="ToggleButton" Property="Background" Value="{StaticResource Theme.ControlHoverBackgroundBrush}"/>
                            <Setter TargetName="ToggleButton" Property="BorderBrush" Value="{StaticResource Theme.ControlHoverBorderBrush}"/>
                        </Trigger>
                        <Trigger Property="IsKeyboardFocusWithin" Value="True">
                            <Setter TargetName="ToggleButton" Property="BorderBrush" Value="{StaticResource Theme.ControlPressedBorderBrush}"/>
                        </Trigger>
                        <Trigger Property="IsDropDownOpen" Value="True">
                            <Setter TargetName="ToggleButton" Property="BorderBrush" Value="{StaticResource Theme.ControlPressedBorderBrush}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="ToggleButton" Property="Opacity" Value="0.5"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Menu / ContextMenu -->
    <Style TargetType="ContextMenu">
        <Setter Property="Background" Value="{StaticResource Theme.SurfaceBackgroundBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource Theme.BorderBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource Theme.PrimaryTextBrush}"/>
        <Setter Property="HasDropShadow" Value="False"/>
        <Setter Property="OverridesDefaultStyle" Value="True"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ContextMenu">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="1"
                            Padding="2"
                            SnapsToDevicePixels="True">
                        <ScrollViewer CanContentScroll="True"
                                      Focusable="False"
                                      Background="{TemplateBinding Background}"
                                      HorizontalScrollBarVisibility="Auto"
                                      VerticalScrollBarVisibility="Auto">
                            <StackPanel IsItemsHost="True"
                                        KeyboardNavigation.DirectionalNavigation="Cycle"/>
                        </ScrollViewer>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="MenuItem">
        <Setter Property="Foreground" Value="{StaticResource Theme.PrimaryTextBrush}"/>
        <Setter Property="Background" Value="{StaticResource Theme.SurfaceBackgroundBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource Theme.BorderBrush}"/>
        <Setter Property="Padding" Value="8,4"/>
        <Setter Property="OverridesDefaultStyle" Value="True"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="MenuItem">
                    <Grid>
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="0"
                                Padding="{TemplateBinding Padding}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="24"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <Rectangle x:Name="CheckArea"
                                           Grid.Column="0"
                                           Fill="{StaticResource Theme.SurfaceBackgroundBrush}"/>

                                <ContentPresenter x:Name="IconHost"
                                                  Grid.Column="0"
                                                  VerticalAlignment="Center"
                                                  HorizontalAlignment="Center"
                                                  ContentSource="Icon"
                                                  RecognizesAccessKey="True"
                                                  Visibility="Collapsed"/>

                                <Path x:Name="CheckGlyph"
                                      Grid.Column="0"
                                      VerticalAlignment="Center"
                                      HorizontalAlignment="Center"
                                      Stroke="{TemplateBinding Foreground}"
                                      StrokeThickness="2"
                                      Data="M 0 4 L 3 7 L 8 0"
                                      Visibility="Collapsed"/>

                                <ContentPresenter Grid.Column="1"
                                                  ContentSource="Header"
                                                  RecognizesAccessKey="True"
                                                  VerticalAlignment="Center"/>

                                <TextBlock x:Name="GestureText"
                                           Grid.Column="2"
                                           Margin="12,0,0,0"
                                           VerticalAlignment="Center"
                                           Foreground="{TemplateBinding Foreground}"
                                           Text="{TemplateBinding InputGestureText}"/>

                                <Path x:Name="Arrow"
                                      Grid.Column="2"
                                      Margin="12,0,0,0"
                                      VerticalAlignment="Center"
                                      Visibility="Collapsed"
                                      Stroke="{TemplateBinding Foreground}"
                                      StrokeThickness="2"
                                      Data="M 0 0 L 4 4 L 0 8"/>
                            </Grid>
                        </Border>

                        <Popup x:Name="PART_Popup"
                               Placement="Right"
                               HorizontalOffset="-2"
                               VerticalOffset="0"
                               AllowsTransparency="False"
                               Focusable="False"
                               IsOpen="{TemplateBinding IsSubmenuOpen}"
                               PopupAnimation="None"
                               SnapsToDevicePixels="True">
                            <Border Background="{StaticResource Theme.SurfaceBackgroundBrush}"
                                    BorderBrush="{StaticResource Theme.BorderBrush}"
                                    BorderThickness="1"
                                    Padding="2"
                                    SnapsToDevicePixels="True">
                                <ScrollViewer CanContentScroll="True"
                                              Style="{x:Null}"
                                              Background="{StaticResource Theme.SurfaceBackgroundBrush}"
                                              HorizontalScrollBarVisibility="Auto"
                                              VerticalScrollBarVisibility="Auto">
                                    <StackPanel IsItemsHost="True"
                                                Background="{StaticResource Theme.SurfaceBackgroundBrush}"
                                                KeyboardNavigation.DirectionalNavigation="Cycle"/>
                                </ScrollViewer>
                            </Border>
                        </Popup>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="Icon" Value="{x:Null}">
                            <Setter TargetName="IconHost" Property="Visibility" Value="Collapsed"/>
                        </Trigger>
                        <Trigger Property="Icon" Value="{x:Null}">
                            <Setter TargetName="CheckGlyph" Property="Margin" Value="0,0,6,0"/>
                        </Trigger>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="CheckGlyph" Property="Visibility" Value="Visible"/>
                        </Trigger>
                        <Trigger Property="InputGestureText" Value="">
                            <Setter TargetName="GestureText" Property="Visibility" Value="Collapsed"/>
                        </Trigger>
                        <Trigger Property="Role" Value="SubmenuHeader">
                            <Setter TargetName="Arrow" Property="Visibility" Value="Visible"/>
                            <Setter TargetName="GestureText" Property="Visibility" Value="Collapsed"/>
                        </Trigger>
                        <Trigger Property="Role" Value="TopLevelHeader">
                            <Setter TargetName="Arrow" Property="Visibility" Value="Visible"/>
                            <Setter TargetName="GestureText" Property="Visibility" Value="Collapsed"/>
                            <Setter TargetName="PART_Popup" Property="Placement" Value="Bottom"/>
                            <Setter Property="Padding" Value="10,6"/>
                            <Setter TargetName="CheckArea" Property="Visibility" Value="Collapsed"/>
                        </Trigger>
                        <Trigger Property="IsSubmenuOpen" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="{StaticResource Theme.ControlHoverBackgroundBrush}"/>
                            <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource Theme.ControlHoverBorderBrush}"/>
                            <Setter TargetName="CheckArea" Property="Fill" Value="{StaticResource Theme.ControlHoverBackgroundBrush}"/>
                        </Trigger>
                        <Trigger Property="IsHighlighted" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="{StaticResource Theme.ControlHoverBackgroundBrush}"/>
                            <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource Theme.ControlHoverBorderBrush}"/>
                            <Setter TargetName="CheckArea" Property="Fill" Value="{StaticResource Theme.ControlHoverBackgroundBrush}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="Bd" Property="Opacity" Value="0.5"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="Separator">
        <Setter Property="Margin" Value="4,2"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Separator">
                    <Border Background="{StaticResource Theme.BorderBrush}"
                            Height="1"
                            SnapsToDevicePixels="True"/>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ScrollBar templates for dark theme -->
    <Style TargetType="ScrollBar">
        <Style.Resources>
            <Style x:Key="ScrollBarLineButtonStyle" TargetType="RepeatButton">
                <Setter Property="Background" Value="{StaticResource Theme.SurfaceAltBackgroundBrush}"/>
                <Setter Property="Foreground" Value="{StaticResource Theme.PrimaryTextBrush}"/>
                <Setter Property="BorderBrush" Value="{StaticResource Theme.BorderBrush}"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="Padding" Value="0"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="RepeatButton">
                            <Border x:Name="Bd"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Bd" Property="Background" Value="{StaticResource Theme.ControlHoverBackgroundBrush}"/>
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter TargetName="Bd" Property="Background" Value="{StaticResource Theme.ControlPressedBackgroundBrush}"/>
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter TargetName="Bd" Property="Opacity" Value="0.4"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style x:Key="ScrollBarThumbStyle" TargetType="Thumb">
                <Setter Property="Background" Value="{StaticResource Theme.ControlHoverBorderBrush}"/>
                <Setter Property="BorderBrush" Value="{StaticResource Theme.ControlPressedBorderBrush}"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="MinHeight" Value="14"/>
                <Setter Property="MinWidth" Value="10"/>
                <Setter Property="Margin" Value="1"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Thumb">
                            <Border x:Name="Bd"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="4"/>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Bd" Property="Background" Value="{StaticResource Theme.ControlPressedBackgroundBrush}"/>
                                    <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource Theme.PrimaryTextBrush}"/>
                                </Trigger>
                                <Trigger Property="IsDragging" Value="True">
                                    <Setter TargetName="Bd" Property="Background" Value="{StaticResource Theme.ControlPressedBackgroundBrush}"/>
                                    <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource Theme.PrimaryTextBrush}"/>
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter TargetName="Bd" Property="Opacity" Value="0.4"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <ControlTemplate x:Key="VerticalScrollBarTemplate" TargetType="ScrollBar">
                <Grid Background="{TemplateBinding Background}" Width="16">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <RepeatButton Style="{StaticResource ScrollBarLineButtonStyle}"
                                  Command="ScrollBar.LineUpCommand"
                                  Content="▲"
                                  Height="16"/>
                    <Track x:Name="PART_Track"
                           Grid.Row="1"
                           IsDirectionReversed="true"
                           Focusable="False">
                        <Track.DecreaseRepeatButton>
                            <RepeatButton Style="{StaticResource ScrollBarLineButtonStyle}" Command="ScrollBar.PageUpCommand" />
                        </Track.DecreaseRepeatButton>
                        <Track.IncreaseRepeatButton>
                            <RepeatButton Style="{StaticResource ScrollBarLineButtonStyle}" Command="ScrollBar.PageDownCommand" />
                        </Track.IncreaseRepeatButton>
                        <Track.Thumb>
                            <Thumb Style="{StaticResource ScrollBarThumbStyle}" Height="30" />
                        </Track.Thumb>
                    </Track>
                    <RepeatButton Style="{StaticResource ScrollBarLineButtonStyle}"
                                  Grid.Row="2"
                                  Command="ScrollBar.LineDownCommand"
                                  Content="▼"
                                  Height="16"/>
                </Grid>
            </ControlTemplate>

            <ControlTemplate x:Key="HorizontalScrollBarTemplate" TargetType="ScrollBar">
                <Grid Background="{TemplateBinding Background}" Height="16">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <RepeatButton Style="{StaticResource ScrollBarLineButtonStyle}"
                                  Command="ScrollBar.LineLeftCommand"
                                  Content="◀"
                                  Width="16"/>
                    <Track x:Name="PART_Track"
                           Grid.Column="1"
                           Focusable="False">
                        <Track.DecreaseRepeatButton>
                            <RepeatButton Style="{StaticResource ScrollBarLineButtonStyle}" Command="ScrollBar.PageLeftCommand" />
                        </Track.DecreaseRepeatButton>
                        <Track.IncreaseRepeatButton>
                            <RepeatButton Style="{StaticResource ScrollBarLineButtonStyle}" Command="ScrollBar.PageRightCommand" />
                        </Track.IncreaseRepeatButton>
                        <Track.Thumb>
                            <Thumb Style="{StaticResource ScrollBarThumbStyle}" Width="30"/>
                        </Track.Thumb>
                    </Track>
                    <RepeatButton Style="{StaticResource ScrollBarLineButtonStyle}"
                                  Grid.Column="2"
                                  Command="ScrollBar.LineRightCommand"
                                  Content="▶"
                                  Width="16"/>
                </Grid>
            </ControlTemplate>
        </Style.Resources>
        <Setter Property="Foreground" Value="{StaticResource Theme.PrimaryTextBrush}"/>
        <Setter Property="Background" Value="{StaticResource Theme.SurfaceBackgroundBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource Theme.BorderBrush}"/>
        <Setter Property="Template" Value="{StaticResource VerticalScrollBarTemplate}"/>
        <Style.Triggers>
            <Trigger Property="Orientation" Value="Horizontal">
                <Setter Property="Template" Value="{StaticResource HorizontalScrollBarTemplate}"/>
            </Trigger>
        </Style.Triggers>
    </Style>
</ResourceDictionary>
