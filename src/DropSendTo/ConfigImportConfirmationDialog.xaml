<Window x:Class="DropSendTo.ConfigImportConfirmationDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        x:ClassModifier="internal"
        Title="Config Import" Height="390" Width="520"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize" WindowStyle="None"
        Background="{DynamicResource Theme.WindowBackgroundBrush}">
    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="32"
                            CornerRadius="0"
                            GlassFrameThickness="0"
                            ResizeBorderThickness="4"
                            UseAeroCaptionButtons="False"/>
    </shell:WindowChrome.WindowChrome>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Height="32" Background="{DynamicResource Theme.SurfaceBackgroundBrush}" MouseLeftButtonDown="OnTitleBarDrag">
            <DockPanel LastChildFill="False">
                <TextBlock Text="{Binding Title, RelativeSource={RelativeSource AncestorType=Window}}"
                           Margin="12,0"
                           VerticalAlignment="Center"
                           FontWeight="SemiBold"
                           Foreground="{DynamicResource Theme.PrimaryTextBrush}"/>
                <Button Content="✕"
                        shell:WindowChrome.IsHitTestVisibleInChrome="True"
                        Width="32"
                        Height="24"
                        Margin="0,4,6,4"
                        DockPanel.Dock="Right"
                        Padding="0"
                        Click="OnCloseClicked"/>
            </DockPanel>
        </Border>

        <Grid Grid.Row="1" Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock x:Name="WarningTitleText"
                       FontSize="16"
                       FontWeight="Bold"
                       Foreground="{DynamicResource Theme.PrimaryTextBrush}"
                       TextWrapping="Wrap"
                       Margin="0,0,0,12"/>
            <TextBlock x:Name="WarningBodyText"
                       Grid.Row="1"
                       Foreground="{DynamicResource Theme.PrimaryTextBrush}"
                       TextWrapping="Wrap"
                       LineHeight="22"
                       Margin="0,0,0,12"/>
            <Border Grid.Row="2"
                    Padding="12"
                    Margin="0,0,0,14"
                    Background="{DynamicResource Theme.SurfaceBackgroundBrush}"
                    CornerRadius="6">
                <TextBlock x:Name="RiskSummaryText"
                           Foreground="{DynamicResource Theme.PrimaryTextBrush}"
                           TextWrapping="Wrap"
                           LineHeight="21"/>
            </Border>
            <CheckBox x:Name="TrustCheckBox"
                      Grid.Row="3"
                      VerticalAlignment="Top"
                      Foreground="{DynamicResource Theme.PrimaryTextBrush}"
                      Checked="OnTrustChanged"
                      Unchecked="OnTrustChanged"/>
            <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
                <Button x:Name="ImportButton" Width="110" Height="32" Margin="4" IsDefault="True" IsEnabled="False" Click="OnImport"/>
                <Button x:Name="CancelButton" Width="90" Height="32" Margin="4" IsCancel="True" Click="OnCancel"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
