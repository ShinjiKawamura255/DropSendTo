<Window x:Class="DropSendTo.ShowLayerPreferenceDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Title="表示レイヤー設定" Height="280" Width="420"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize" WindowStyle="None"
        Background="{DynamicResource Theme.WindowBackgroundBrush}">
    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="32"
                            CornerRadius="0"
                            GlassFrameThickness="0"
                            ResizeBorderThickness="4"
                            UseAeroCaptionButtons="False"/>
    </shell:WindowChrome.WindowChrome>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Height="32" Background="{DynamicResource Theme.SurfaceBackgroundBrush}" MouseLeftButtonDown="OnTitleBarDrag">
            <DockPanel LastChildFill="False">
                <TextBlock Text="{Binding Title, RelativeSource={RelativeSource AncestorType=Window}}"
                           Margin="12,0"
                           VerticalAlignment="Center"
                           FontWeight="SemiBold"
                           Foreground="{DynamicResource Theme.PrimaryTextBrush}"/>
                <Button Content="✕"
                        shell:WindowChrome.IsHitTestVisibleInChrome="True"
                        Width="32"
                        Height="24"
                        Margin="0,4,6,4"
                        DockPanel.Dock="Right"
                        Padding="0"
                        Click="OnCloseClicked"/>
            </DockPanel>
        </Border>

        <Grid Margin="14" Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <TextBlock Text="マウスジェスチャで表示する時" Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2"
                       FontWeight="SemiBold" Margin="0,0,0,6"/>

            <TextBlock Text="最小化/非表示から表示" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,8,6"/>
            <ComboBox x:Name="MouseHiddenCombo" Grid.Row="1" Grid.Column="1" Margin="0,0,0,6" />

            <TextBlock Text="表示中に呼び出し" Grid.Row="2" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,8,6"/>
            <ComboBox x:Name="MouseVisibleCombo" Grid.Row="2" Grid.Column="1" Margin="0,0,0,12" />

            <TextBlock Text="PREFIX Enter で表示する時" Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="2"
                       FontWeight="SemiBold" Margin="0,0,0,6"/>

            <TextBlock Text="最小化/非表示から表示" Grid.Row="4" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,8,6"/>
            <ComboBox x:Name="PrefixHiddenCombo" Grid.Row="4" Grid.Column="1" Margin="0,0,0,6" />

            <TextBlock Text="表示中に呼び出し" Grid.Row="5" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,8,6"/>
            <ComboBox x:Name="PrefixVisibleCombo" Grid.Row="5" Grid.Column="1" Margin="0,0,0,8" />

            <StackPanel Orientation="Horizontal" Grid.Row="6" Grid.Column="0" Grid.ColumnSpan="2"
                        HorizontalAlignment="Right" Margin="0,10,0,0">
                <Button Content="OK" Width="90" Height="32" Margin="4" IsDefault="True" Click="OnOk"/>
                <Button Content="Cancel" Width="90" Height="32" Margin="4" IsCancel="True" Click="OnCancel"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
