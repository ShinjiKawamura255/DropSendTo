<Window x:Class="DropSendTo.SlotShortcutEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        x:ClassModifier="internal"
        Title="ショートカット変更"
        MinHeight="220"
        Width="420"
        SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        WindowStyle="None"
        ShowInTaskbar="False"
        Background="{DynamicResource Theme.WindowBackgroundBrush}">
    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="32"
                            CornerRadius="0"
                            GlassFrameThickness="0"
                            ResizeBorderThickness="4"
                            UseAeroCaptionButtons="False"/>
    </shell:WindowChrome.WindowChrome>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Height="32"
                Background="{DynamicResource Theme.SurfaceBackgroundBrush}"
                MouseLeftButtonDown="OnTitleBarDrag">
            <DockPanel LastChildFill="False">
                <TextBlock Text="{Binding Title, RelativeSource={RelativeSource AncestorType=Window}}"
                           Margin="12,0"
                           VerticalAlignment="Center"
                           FontWeight="SemiBold"
                           Foreground="{DynamicResource Theme.PrimaryTextBrush}"/>
                <Button Content="✕"
                        shell:WindowChrome.IsHitTestVisibleInChrome="True"
                        Width="32"
                        Height="24"
                        Margin="0,4,6,4"
                        DockPanel.Dock="Right"
                        Padding="0"
                        Click="OnCloseClicked"/>
            </DockPanel>
        </Border>

        <Grid Grid.Row="1" Margin="16">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock x:Name="SlotText"
                       Grid.Row="0"
                       Margin="0 0 0 6"
                       FontWeight="SemiBold"
                       TextWrapping="Wrap"/>

            <TextBlock Grid.Row="1"
                       Margin="0 0 0 6"
                       Text="ショートカットを入力してください。空欄で解除されます。"
                       Foreground="{DynamicResource Theme.SecondaryTextBrush}"
                       FontSize="11"
                       TextWrapping="Wrap"/>

            <TextBox x:Name="ShortcutBox"
                     Grid.Row="2"
                     MinWidth="240"
                     Margin="0 0 0 10"
                     TextChanged="OnShortcutChanged"/>

            <TextBlock x:Name="ErrorText"
                       Grid.Row="3"
                       Margin="0 0 0 10"
                       Foreground="{DynamicResource Theme.ValidationTextBrush}"
                       TextWrapping="Wrap"
                       Visibility="Collapsed"/>

            <StackPanel Grid.Row="4"
                        Orientation="Horizontal"
                        HorizontalAlignment="Right">
                <Button x:Name="OkButton"
                        Content="OK"
                        Width="90"
                        Margin="0 0 8 0"
                        IsDefault="True"
                        Click="OnOk"/>
                <Button Content="キャンセル"
                        Width="90"
                        IsCancel="True"
                        Click="OnCancel"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
